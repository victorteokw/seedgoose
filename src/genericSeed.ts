import { GeneralSeedCommand } from './SeedingCommand';

const genericSeed: GeneralSeedCommand = async function (
  collectionName, records, options, mongoose, reporter, command
) {

}

export default genericSeed;
